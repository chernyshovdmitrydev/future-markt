//использовал vue 3, так как он является фаворитом из фреймворков которые я знаю.
//сборщик vite предпочитаю, из-за скорости.

import { createApp } from 'vue'
import './style.scss'
import App from './App.vue'

createApp(App).mount('#app')
